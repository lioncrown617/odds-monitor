[phases.setup]
nixPkgs = ["nodejs", "python311"]

[phases.install]
cmds = [
  "pip install flask requests",
  "cd hkjc-bridge && npm install"
]

[start]
cmd = "node hkjc-bridge/server.js & sleep 3 && python odd.py"
